#include "main.h"
/**
 * _strlen - prints the length of a string
 * @s: is the string
 *
 * Return: length of the string
 */
int _strlen(char *s)
{
	int len;
	int i;

	for (i = 1; s[i - 1] != '\0'; i++)
	{
		len = i;
	}
	return (len);
}
